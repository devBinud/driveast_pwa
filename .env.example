# DriveEast Frontend API Configuration
VITE_API_BASE_URL=https://api.driveast.com/api/v1
VITE_WS_BASE_URL=wss://api.driveast.com/api/v1

# Web Push VAPID public key (safe to expose; pair with VAPID_PRIVATE_KEY on the backend)
VITE_VAPID_PUBLIC_KEY=
